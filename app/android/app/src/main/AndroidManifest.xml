<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="barista_voice"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- App-wide deep link: baristavoice://open/<any path>. `open`
                 is a fixed, meaningless host — Android's intent-filter data
                 matching only applies pathPrefix when a host is present, and
                 go_router matches purely on the URI path, so the real
                 destination needs to land in the path, not the host. A
                 custom scheme needs no domain verification (unlike https
                 App Links), so this works immediately without hosting an
                 assetlinks.json — the tradeoff is the OS won't offer this
                 app for a plain https://.../whatever link unless App Links
                 are configured separately later.
                 No pathPrefix is set (deliberately, as of the Stripe usage
                 billing setup flow) so this one intent-filter matches ANY
                 path under the "open" host — both the café QR/deep link
                 (baristavoice://open/cafe/{cafeId}, see CafeLinks in
                 app/lib/core/cafe_links.dart) and the Stripe Checkout
                 setup-mode success/cancel redirect
                 (baristavoice://open/admin/billing, see
                 stripe_billing_repository.dart), and anything added later,
                 without needing a second <data> element per destination. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="baristavoice" android:host="open"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
